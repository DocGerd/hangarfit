# v0.6.0 Solver Polish — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close the four `/pr-review` follow-ups (#92, #111, #98, #95) carried over from Phase 2a, then cut `v0.6.0`. No algorithm changes; no new features; no operational-readiness work.

**Architecture:** Two parallel waves of subagent-driven per-PR work. Wave 1 lands two foundation additions in parallel (`SolverDiagnostics` fields and `SearchConfig.max_restarts`); Wave 2 lands two consumer PRs in parallel (each leveraging a Wave-1 addition). Each PR is reviewed via the `pr-review-toolkit:review-pr` skill before being handed back for user approval and merge. The release cut runs the `/release-cut` skill once Wave 2 lands.

**Tech Stack:** Python 3.11+, frozen dataclasses w/ slots, pytest + `caplog` + `tmp_path`, ruff, mypy, `gh` CLI + `gh api graphql`, git worktrees for parallel dispatch.

**Spec reference:** `docs/superpowers/specs/2026-05-22-v0.6.0-solver-polish-release-design.md`. Disagreements between this plan and the spec should be resolved by updating the plan, not by ad-libbing.

---

## Plan structure & GitFlow

Per spec §5, work is two parallel waves. Each wave has two independent issues. Per CLAUDE.md GitFlow:

- One GitHub issue per PR (already filed: #92, #95, #98, #111 — all in milestone `v0.6.0 — Solver polish`).
- Each PR's branch is `feature/<n>-<slug>` cut off the latest `develop`. Wave-2 branches are cut off post-Wave-1 `develop`.
- Each implementer subagent runs in its own git worktree to isolate parallel work.
- After implementation, main session runs `/pr-review`, converts findings to threads, resolves them, hands back to user. Never `gh pr merge` from Claude.

| Wave | Task | Issue | Branch | Worktree | Foundation? |
|---|---|---|---|---|---|
| 1 | Task 1 | #92 | `feature/92-diversity-observability` | `/home/pkuhn/hangarfit-92-diversity-observability` | Adds `SolverDiagnostics` fields |
| 1 | Task 2 | #111 | `feature/111-max-restarts` | `/home/pkuhn/hangarfit-111-max-restarts` | Adds `SearchConfig.max_restarts` |
| — | Task 3 | (none) | — | — | Wave-1 → Wave-2 sync gate |
| 2 | Task 4 | #98 | `feature/98-fixture-matrix-fail-loud` | `/home/pkuhn/hangarfit-98-fixture-matrix-fail-loud` | Consumes #111 |
| 2 | Task 5 | #95 | `feature/95-chunk-f-cleanup` | `/home/pkuhn/hangarfit-95-chunk-f-cleanup` | Consumes #92 |
| — | Task 6 | (none) | `release/0.6.0` | (main session) | Release cut |

**Working directory.** Wave 1 main-session orchestration runs from `/home/pkuhn/hangarfit` on `develop`. Subagents dispatched into worktrees treat their worktree root as cwd. Per `feedback_development_workflow` and global CLAUDE.md, inside a worktree use bare `git <cmd>` — never `git -C <worktree-path>`, which breaks allowlist matching on sub-commands.

**Acceptance for "task done":** all checkboxes ticked; `pytest -m ""` (full suite incl. slow) green; `ruff check src/ tests/`, `ruff format --check src/ tests/`, `mypy src/hangarfit/` all clean; `/pr-review` findings all resolved per `feedback_resolve_review_threads`; user has approved and merged the PR to `develop`.

---

## Task 0: Pre-flight verification (main session)

**Files:** none (verification only).

- [ ] **Step 1: Confirm milestone state**

```bash
gh api repos/:owner/:repo/milestones/10 --jq '{number, title, state, open_issues, closed_issues}'
```

Expected: `{"number":10,"title":"v0.6.0 — Solver polish","state":"open","open_issues":5,"closed_issues":0}`. The 5 open issues are #92, #95, #98, #111 (the four follow-ups) plus #115 (this spec+plan release-planning issue itself).

- [ ] **Step 2: Confirm develop is clean and up-to-date**

```bash
git status
git switch develop
git pull --ff-only
git log -1 --oneline
```

Expected: clean working tree, develop matches origin/develop.

- [ ] **Step 3: Confirm the spec doc is in place**

```bash
ls -1 docs/superpowers/specs/2026-05-22-v0.6.0-solver-polish-release-design.md
```

Expected: file exists. If missing, abort — the spec is upstream of every Task below.

---

## Wave 1 (parallel)

Wave 1 dispatches **simultaneously** — two implementer subagents run in two worktrees. Main session creates both worktrees, then dispatches both subagents in a single message with two `Agent` tool calls (the parallel-dispatch pattern documented in `superpowers:dispatching-parallel-agents`).

### Wave 1 setup (main session)

- [ ] **Step 1: Create Wave-1 worktrees**

```bash
git worktree add ../hangarfit-92-diversity-observability -b feature/92-diversity-observability develop
git worktree add ../hangarfit-111-max-restarts -b feature/111-max-restarts develop
git worktree list
```

Expected: two new worktrees listed, each on its own branch off `develop`.

- [ ] **Step 2: Dispatch both implementer subagents in parallel**

Send a single message with two `Agent` tool calls:

- First call: subagent for Task 1 (#92), cwd = `../hangarfit-92-diversity-observability`, brief = Task 1's `Implementer brief` block below.
- Second call: subagent for Task 2 (#111), cwd = `../hangarfit-111-max-restarts`, brief = Task 2's `Implementer brief` block below.

Wait for both to return. They are fully independent — no cross-talk required.

---

## Task 1: Issue #92 — Diversity observability on `SolverDiagnostics`

**Issue:** [#92](https://github.com/DocGerd/hangarfit/issues/92)
**Branch:** `feature/92-diversity-observability`
**Worktree:** `../hangarfit-92-diversity-observability`
**Spec section:** §4.1

**Files:**
- Modify: `src/hangarfit/models.py` (`SolverDiagnostics` dataclass at line 576-610: add two fields + extend `__post_init__`)
- Modify: `src/hangarfit/solver.py` (warning site around line 80-87; diversity filter accept/reject site around line 172-176; both `SolverDiagnostics(...)` construction sites around line 98-104, 222-231, 234-243 — set the new fields)
- Modify: `tests/test_solver_search.py` (extend existing diversity-impossible warning test to assert `r.diagnostics.diversity_impossible is True`)
- Add: at least one new test in `tests/test_solver_search.py` that produces `diversity_rejected_count > 0`
- Modify: `tests/test_models.py` (default-value test for both new fields; post_init guard test for negative `diversity_rejected_count`)

### Implementer brief (paste verbatim into the Task 1 subagent dispatch)

> **You are implementing issue #92 in the hangarfit repository.** Your cwd is the worktree `../hangarfit-92-diversity-observability` on branch `feature/92-diversity-observability` (already created off latest `develop`). Use bare `git`, `pytest`, `ruff`, `mypy` — never `git -C`.
>
> **Source of truth:** `docs/superpowers/specs/2026-05-22-v0.6.0-solver-polish-release-design.md` §4.1. The spec block below is the exact scope; nothing more, nothing less.
>
> **Spec §4.1 scope:**
> Adds (`src/hangarfit/models.py`):
> - `SolverDiagnostics.diversity_impossible: bool = False` — `True` iff the static `K > 1 ∧ planes_count == 0 ∧ everything-pinned` precondition fires (i.e., the site that already logs the diversity-impossible warning at `solver.py:80-87`).
> - `SolverDiagnostics.diversity_rejected_count: int = 0` — incremented whenever the diversity filter rejects a candidate that would otherwise have been accepted (the reject branch inside `solve()`'s valid-layout block at `solver.py:172-176`).
> - `__post_init__` guard: `diversity_rejected_count >= 0`.
>
> Modifies (`src/hangarfit/solver.py`):
> - Set `diversity_impossible = True` at the same site that already logs the static warning.
> - Increment `diversity_rejected_count` inside the diversity filter's reject branch.
>
> Tests (`tests/test_solver_search.py`):
> - Extend the existing diversity-impossible warning test (search `caplog` for the diversity warning) to also assert `r.diagnostics.diversity_impossible is True`.
> - Add a focused test that runs a fixture where the diversity filter rejects at least one candidate (use `solve_diversity_impossible_warn.yaml` or `solve_fresh_alternatives_three.yaml` — pick the one whose K>1 + small-fleet config most reliably forces rejection, or construct a small fixture inline), then asserts `r.diagnostics.diversity_rejected_count > 0`.
> - Also add `tests/test_models.py` cases: default `diversity_impossible is False`, default `diversity_rejected_count == 0`, and `__post_init__` raises `ValueError` when `diversity_rejected_count = -1`.
>
> **TDD discipline.** For each new behavior: write the failing test → run it → see it fail → add minimal code → see it pass → commit. Do NOT bundle multiple behaviors into one commit; smaller commits make `/pr-review` cleaner. Commit messages follow `feat(models): ...` / `feat(solver): ...` / `test(...): ...` style.
>
> **Acceptance gates before push:**
> - `pytest -m ""` green (slow suite included).
> - `ruff check src/ tests/` clean.
> - `ruff format --check src/ tests/` clean.
> - `mypy src/hangarfit/` clean.
> - No additions beyond spec §4.1 (no scope creep; if you see adjacent issues, file them as fresh issues — don't fix them in this PR).
>
> **Open the PR.** Base `develop`, title `feat(solver): expose diversity observability on SolverDiagnostics (#92)`, body uses the template at the end of this brief, then assignee=DocGerd, label=enhancement, milestone=`v0.6.0 — Solver polish` (number 10) via `gh pr create` + `gh api -X PATCH` per `feedback_pr_metadata`. Print the PR URL when done.
>
> **PR body template (use a HEREDOC):**
> ```
> Closes #92
>
> Implements spec §4.1 of `docs/superpowers/specs/2026-05-22-v0.6.0-solver-polish-release-design.md`.
>
> ## Changes
>
> - `SolverDiagnostics` gains `diversity_impossible: bool = False` and `diversity_rejected_count: int = 0`.
> - `solver.py` sets `diversity_impossible = True` at the static-precondition warning site and increments `diversity_rejected_count` in the diversity filter's reject branch.
> - Tests: extended existing diversity-warning test; new tests for `diversity_rejected_count > 0` path and model-level defaults / guard.
>
> ## Out of scope
>
> Anything beyond spec §4.1 — see issue #115 for the v0.6.0 release plan.
> ```

### Task 1 verification (main session, after subagent returns)

- [ ] **Step 1: Spec-compliance review of the diff**

In the main session (cwd = repo root), inspect the PR diff:

```bash
gh pr diff <pr-number-from-subagent>
```

Check every change against spec §4.1. Reject anything outside scope.

- [ ] **Step 2: Invoke `/pr-review`**

```
/pr-review <pr-number>
```

Likely applicable agents: `code-reviewer` (always), `type-design-analyzer` (because `models.py` schema added), `pr-test-analyzer` (test additions). The `geometry-invariant-guard` does NOT apply (no `geometry.py` or `collisions.py` changes).

- [ ] **Step 3: Convert findings to inline review threads**

For each finding, post via `gh api repos/:owner/:repo/pulls/<n>/comments` with `commit_id` + `path` + `line` per the diff. The reviewers' output already names the file:line.

- [ ] **Step 4: Resolve every thread**

Fix code (preferred) or reply-with-rationale. Then mark each thread resolved via GraphQL `resolveReviewThread` per `feedback_resolve_review_threads`.

- [ ] **Step 5: Hand back to user**

> "PR #<n> clean and ready for final review."

---

## Task 2: Issue #111 — `SearchConfig.max_restarts` + best-partial canary

**Issue:** [#111](https://github.com/DocGerd/hangarfit/issues/111)
**Branch:** `feature/111-max-restarts`
**Worktree:** `../hangarfit-111-max-restarts`
**Spec section:** §4.2

**Files:**
- Modify: `src/hangarfit/models.py` (`SearchConfig` dataclass at line 665-695: add `max_restarts: int | None = None` + extend `__post_init__`)
- Modify: `src/hangarfit/solver.py` (restart-loop condition around line 124: gate on `max_restarts` in addition to `budget_s`)
- Add: new canary test in `tests/test_solver_canaries.py` for the deterministic `best_partial_layout` case
- Modify: `tests/test_models.py` (default-value test for `max_restarts`; post_init guard test for `max_restarts = 0`)

### Implementer brief (paste verbatim into the Task 2 subagent dispatch)

> **You are implementing issue #111 in the hangarfit repository.** Your cwd is the worktree `../hangarfit-111-max-restarts` on branch `feature/111-max-restarts` (already created off latest `develop`). Use bare `git`, `pytest`, `ruff`, `mypy` — never `git -C`.
>
> **Source of truth:** `docs/superpowers/specs/2026-05-22-v0.6.0-solver-polish-release-design.md` §4.2. The spec block below is the exact scope; nothing more, nothing less.
>
> **Spec §4.2 scope:**
> Adds (`src/hangarfit/models.py`):
> - `SearchConfig.max_restarts: int | None = None` — `None` preserves current wall-clock-only behavior.
> - `__post_init__` guard: when set, `max_restarts >= 1`.
>
> Modifies (`src/hangarfit/solver.py`):
> - Restart loop honors `max_restarts` as an upper-bound counter in addition to the wall-clock budget. First gate to trip wins. Concretely, the outer `while time.monotonic() - start < budget_s:` at `solver.py:124` becomes `while (time.monotonic() - start < budget_s) and (search.max_restarts is None or restart_index < search.max_restarts):` (or equivalent — readable expression preferred over compact).
> - No algorithm change otherwise — same RR-MC descent, same seed handling, same diversity filter.
>
> Tests (`tests/test_solver_canaries.py`):
> - New canary `test_solve_deterministic_best_partial_under_max_restarts`: load `tests/fixtures/solve_fresh_six_planes.yaml`, run `solve(..., budget_s=30.0, seed=42, search=SearchConfig(max_restarts=3))` twice; assert `r.status == "exhausted_budget"`, `r.best_partial_layout is not None`, and `r1.diagnostics.best_partial_layout.placements == r2.diagnostics.best_partial_layout.placements`. The 30-s budget is generous — `max_restarts=3` will trip first since fresh_six_planes typically needs many more restarts to find a layout.
> - If `max_restarts=3` actually finds a layout on this fixture (lucky run), bump the candidate value down to 1 or pick a fixture from `solve_diversity_impossible_warn.yaml` / `solve_infeasible_*.yaml` that more reliably exhausts.
> - The existing wall-clock-budgeted canaries (`test_solve_deterministic_given_seed` over `CANARY_FIXTURES`) stay unchanged.
> - Also add `tests/test_models.py` cases: default `SearchConfig().max_restarts is None`, `SearchConfig(max_restarts=1)` accepted, `SearchConfig(max_restarts=0)` raises `ValueError`, `SearchConfig(max_restarts=-1)` raises `ValueError`.
>
> **TDD discipline.** Per Task 1's brief. Same commit-style.
>
> **Acceptance gates before push:** Same as Task 1's brief.
>
> **Open the PR.** Base `develop`, title `feat(solver): add SearchConfig.max_restarts and best_partial determinism canary (#111)`, body via HEREDOC, metadata via `gh pr create` + `gh api -X PATCH` for milestone 10. Print PR URL.
>
> **PR body template:**
> ```
> Closes #111
>
> Implements spec §4.2 of `docs/superpowers/specs/2026-05-22-v0.6.0-solver-polish-release-design.md`.
>
> ## Changes
>
> - `SearchConfig.max_restarts: int | None = None` — None preserves current wall-clock-only behavior.
> - `solver.py` outer restart loop honors `max_restarts` in addition to `budget_s`; first gate to trip wins.
> - New canary in `tests/test_solver_canaries.py` pinning a deterministic `best_partial_layout` via `max_restarts`.
> - `tests/test_models.py` cases for default + guard.
>
> ## Out of scope
>
> Anything beyond spec §4.2.
> ```

### Task 2 verification (main session, after subagent returns)

Identical steps to Task 1's verification block, applied to Task 2's PR number. Applicable `/pr-review` agents: `code-reviewer`, `type-design-analyzer` (schema change), `pr-test-analyzer` (canary test), `silent-failure-hunter` (any changes to `solver.py` per project convention — see CLAUDE.md "Subagents" section).

---

## Task 3: Wave 1 → Wave 2 synchronization gate (main session)

**Files:** none (synchronization only).

- [ ] **Step 1: Wait for both Wave-1 PRs to be merged by the user**

Do not dispatch Wave 2 until BOTH Task 1's PR and Task 2's PR are merged to `develop`. Verify:

```bash
gh pr view <task1-pr> --json state,mergedAt --jq '{state, mergedAt}'
gh pr view <task2-pr> --json state,mergedAt --jq '{state, mergedAt}'
```

Expected: both `{"state":"MERGED","mergedAt":"<timestamp>"}`.

- [ ] **Step 2: Sync main-session develop**

```bash
git switch develop
git pull --ff-only
git log -3 --oneline
```

Expected: the two merge commits are at the tip; develop fast-forwarded.

- [ ] **Step 3: Confirm Wave-1 worktrees can be reaped**

```bash
git worktree list
```

If `../hangarfit-92-diversity-observability` or `../hangarfit-111-max-restarts` still show, run the `commit-commands:clean_gone` skill — they should be `[gone]` once the user merged + GitHub deleted the head branches.

If you prefer manual cleanup:

```bash
git worktree remove ../hangarfit-92-diversity-observability
git worktree remove ../hangarfit-111-max-restarts
git branch -d feature/92-diversity-observability
git branch -d feature/111-max-restarts
```

Expected: worktrees and local branches removed; only the merged commits remain on `develop`.

---

## Wave 2 (parallel)

Wave 2 dispatches **simultaneously** — two implementer subagents in two worktrees, both branched off the *new* `develop` (which now contains the Wave-1 additions).

### Wave 2 setup (main session)

- [ ] **Step 1: Create Wave-2 worktrees**

```bash
git worktree add ../hangarfit-98-fixture-matrix-fail-loud -b feature/98-fixture-matrix-fail-loud develop
git worktree add ../hangarfit-95-chunk-f-cleanup -b feature/95-chunk-f-cleanup develop
git worktree list
```

Expected: two new worktrees off the post-Wave-1 `develop` tip.

- [ ] **Step 2: Dispatch both implementer subagents in parallel**

Single message, two `Agent` tool calls:

- First: subagent for Task 4 (#98), cwd = `../hangarfit-98-fixture-matrix-fail-loud`, brief = Task 4's `Implementer brief`.
- Second: subagent for Task 5 (#95), cwd = `../hangarfit-95-chunk-f-cleanup`, brief = Task 5's `Implementer brief`.

Wait for both.

---

## Task 4: Issue #98 — Fixture-matrix tests fail loudly on regression

**Issue:** [#98](https://github.com/DocGerd/hangarfit/issues/98)
**Branch:** `feature/98-fixture-matrix-fail-loud`
**Worktree:** `../hangarfit-98-fixture-matrix-fail-loud`
**Spec section:** §4.3
**Wave-1 dependency:** uses `SearchConfig.max_restarts` from #111.

**Files:**
- Modify: `tests/test_solver_fixture_matrix.py` (four `pytest.skip(...)` call sites near lines 84, 120, 157, 208 — replace with assertion-driven exhaustion; also remove the dead `if r.status == "found_partial"` branch in the `_assert_universal_properties` helper)

### Implementer brief (paste verbatim into the Task 4 subagent dispatch)

> **You are implementing issue #98 in the hangarfit repository.** Your cwd is the worktree `../hangarfit-98-fixture-matrix-fail-loud` on branch `feature/98-fixture-matrix-fail-loud` (already created off post-Wave-1 `develop`). Use bare `git`, `pytest`, `ruff`, `mypy` — never `git -C`.
>
> **Source of truth:** `docs/superpowers/specs/2026-05-22-v0.6.0-solver-polish-release-design.md` §4.3. The spec block below is the exact scope; nothing more, nothing less.
>
> **Spec §4.3 scope:**
> Modifies (`tests/test_solver_fixture_matrix.py`):
> - Each affected matrix test now uses `SearchConfig(max_restarts=K)` (from Wave 1's #111) where K is chosen so the fixture is known to succeed within K restarts on a healthy solver, with comfortable headroom.
> - **K is pinned per fixture by recording observed restart-counts under `seed=42` then setting `K = max(observed × 2, 5)`.** Procedure:
>   1. Run each affected matrix test under the current solver (no `max_restarts`) and collect `r.diagnostics.restarts_attempted` when it finds a layout. Do this 3 times to detect variance.
>   2. Pick `K = max(observed_max × 2, 5)` for that fixture.
>   3. Record the K values + observed counts in a comment block above each modified test for future-you.
> - On `exhausted_budget`, the test fails with a diagnostic message identifying the fixture (instead of `pytest.skip`):
>   ```python
>   assert r.status == "found", (
>       f"Fixture {fixture_path!r} exhausted within max_restarts={K}; "
>       f"a regression is likely (was previously found within {observed} restarts under seed=42)."
>   )
>   ```
> - Drop the dead `if r.status == "found_partial"` branch in `_assert_universal_properties` — unreachable under the v1 matrix since no v1 fixture produces `found_partial`. If a future fixture needs partial-result handling, the branch can come back; right now it's dead code.
>
> **TDD discipline.** For each affected test:
> 1. Write the assertion in the failing form first (`assert r.status == "found", ...`) WITHOUT yet adding `max_restarts`. Run the test to confirm it now fails-instead-of-skips when the fixture exhausts under the existing budget (you may need to drop `budget_s` to force exhaustion for verification, then restore).
> 2. Add `SearchConfig(max_restarts=K)` with K chosen per the procedure above.
> 3. Run the test; expect PASS.
> 4. Commit per-test.
>
> **Acceptance gates before push:** Same as Task 1's brief. Plus: `pytest tests/test_solver_fixture_matrix.py -v` must show ALL six matrix tests as PASS (no SKIP, no XFAIL).
>
> **Open the PR.** Base `develop`, title `test(solver): make fixture-matrix fail loudly on regression (#98)`, body via HEREDOC, metadata via `gh pr create` + `gh api -X PATCH` for milestone 10. Print PR URL.
>
> **PR body template:**
> ```
> Closes #98
>
> Implements spec §4.3 of `docs/superpowers/specs/2026-05-22-v0.6.0-solver-polish-release-design.md`.
>
> ## Changes
>
> - Four matrix tests (`test_*` in `tests/test_solver_fixture_matrix.py`) use `SearchConfig(max_restarts=K)` from #111 to make exhaustion deterministic across machines.
> - `pytest.skip("exhausted_budget")` replaced with `assert r.status == "found"` — regressions now fail loudly instead of silently skipping.
> - Per-fixture K values recorded in comments above each test (`K = max(observed × 2, 5)`).
> - Dropped dead `if r.status == "found_partial"` branch in `_assert_universal_properties` helper (unreachable under v1 matrix).
>
> ## Out of scope
>
> Anything beyond spec §4.3.
> ```

### Task 4 verification (main session, after subagent returns)

Identical structure to Task 1's verification. Applicable `/pr-review` agents: `code-reviewer` (always), `pr-test-analyzer` (test-heavy diff). Likely NOT applicable: `type-design-analyzer` (no schema change), `geometry-invariant-guard` (no geometry change).

---

## Task 5: Issue #95 — Chunk F cleanup bundle

**Issue:** [#95](https://github.com/DocGerd/hangarfit/issues/95)
**Branch:** `feature/95-chunk-f-cleanup`
**Worktree:** `../hangarfit-95-chunk-f-cleanup`
**Spec section:** §4.4
**Wave-1 dependency:** can optionally assert on `SolverDiagnostics.diversity_*` fields from #92.

**Files:**
- Modify: `src/hangarfit/cli.py` (matplotlib comment at L≈240-242 inside `cmd_solve`; `_HUMAN_SHIFT_THRESHOLD_M = 0.5` constant at L≈241; per-function `§5.x` docstring references in `cmd_solve`, `_emit_solve_human`, `_emit_solve_json`)
- Modify: `tests/test_cli_solve.py` (rename misnamed `test_human_output_exhausted_budget`; add new tests for `exhausted_budget` human branch, K>1 happy path with `{i}` substitution, `--fleet`/`--hangar` override branches, `OSError`-during-write rc=2 path, `maintenance_plane` round-trip)

### Implementer brief (paste verbatim into the Task 5 subagent dispatch)

> **You are implementing issue #95 in the hangarfit repository.** Your cwd is the worktree `../hangarfit-95-chunk-f-cleanup` on branch `feature/95-chunk-f-cleanup` (already created off post-Wave-1 `develop`). Use bare `git`, `pytest`, `ruff`, `mypy` — never `git -C`.
>
> **Source of truth:** `docs/superpowers/specs/2026-05-22-v0.6.0-solver-polish-release-design.md` §4.4. The spec block below is the exact scope.
>
> **Spec §4.4 scope — code/comment fixes:**
> 1. **Matplotlib comment honesty fix** at `src/hangarfit/cli.py:240-242` in `cmd_solve`. The current comment claims local imports defer matplotlib cost, but `cli.py:20` does `from hangarfit import visualize` at module top, and `visualize.py:28-33` eagerly imports matplotlib + sets the Agg backend. **Pick ONE of two fixes:**
>    - (a) Move `from hangarfit import visualize` to a local import inside `cmd_solve` so the comment becomes true; or
>    - (b) Rewrite the comment to honestly say "defer the solver import only — matplotlib is already loaded via `visualize` at module top."
>    Prefer (b) — the cost of (a) is unclear (other CLI paths use `visualize`) and the comment fix is sufficient.
> 2. **`_HUMAN_SHIFT_THRESHOLD_M = 0.5` brittle duplicate** at `cli.py:241`. Replace with a reference to `DiversityConfig().position_threshold_m` (a class instantiation is fine — `DiversityConfig` is a frozen dataclass with no side effects) so the human-output narration cannot diverge from the diversity filter that gated acceptance. Alternative: keep the local constant but add a guard test asserting `_HUMAN_SHIFT_THRESHOLD_M == DiversityConfig().position_threshold_m`. **Prefer the reference**: one less knob to keep in sync.
> 3. **Per-function `§5.x` spec references** in docstrings of `cmd_solve`, `_emit_solve_human`, `_emit_solve_json` — delete. Module-level "See spec for details" is fine; per-function pointers add the most rot surface and least value.
>
> **Spec §4.4 scope — missing test coverage:**
> 4. **`exhausted_budget` human-output coverage.** The real `_emit_solve_human` branch for `exhausted_budget` (printing "No valid layout found in {time}s", "Best partial had N conflicts:", "Hint: increase --budget") has zero coverage today. Use `solve_fresh_six_planes.yaml` with `--budget 0.05`; capture stdout via `capsys`; assert all three lines present. Follow the skip-on-lucky guard pattern at `tests/test_solver_search.py:355-391` so a fast machine doesn't accidentally find a layout.
> 5. **Rename `test_human_output_exhausted_budget`** at `tests/test_cli_solve.py:123-131` — it actually tests `trivially_infeasible`, not `exhausted_budget` (the inline comment in the test admits this). Rename to `test_human_output_trivially_infeasible` (or split into two clearer tests if you also added #4's coverage). Update any internal cross-references.
> 6. **K>1 happy path with `{i}` substitution.** Run `solve --alternatives 2 --render-pattern out_{i}.png --write-yaml-pattern out_{i}.yaml`; assert files `out_1.png`, `out_2.png`, `out_1.yaml`, `out_2.yaml` all exist (use `tmp_path` for output directory). The current K=1 substitute test only exercises `i=1`; this exercises the loop body's `enumerate(layouts, start=1)` at multiple values.
> 7. **`--fleet` / `--hangar` override coverage.** Exercise the four branches of `_resolve_fleet_hangar_refs` (no override / fleet only / hangar only / both); at minimum, one positive override test and one collision-rejection test (override + embedded ref → error).
> 8. **`OSError`-during-write rc=2.** `solve --render /nonexistent_dir/out.png` should yield `rc == 2` and `"error:"` in stderr. Tiny test.
> 9. **`maintenance_plane` round-trip.** The `payload["maintenance"] = {"plane": ...}` branch at `cli.py:428-429` is unexercised because the existing `test_write_yaml_roundtrips_via_check` uses a single-Husky fixture with no maintenance block. Add a coverage test that uses a fixture with a maintenance plane (any `solve_maintenance_bay_required.yaml` or `solve_pinned_one_plane.yaml` variant), writes the layout YAML, then loads it via `loader.load_layout` and verifies the maintenance plane survives.
>
> **TDD discipline.** Per-bullet commits — nine items above means up to nine commits (or fewer if you bundle thin ones). Smaller commits make `/pr-review` cleaner.
>
> **Scope discipline.** This is the largest scope-creep risk of all four PRs. Do NOT fix adjacent issues you spot. If you find something, file it as a fresh issue and link it from the PR body; don't expand this PR.
>
> **Acceptance gates before push:** Same as Task 1's brief. Plus: every bullet above (1-9) has a corresponding commit (or grouped commit) and the diff demonstrably covers it.
>
> **Open the PR.** Base `develop`, title `chore(cli): Chunk F cleanup bundle (#95)`, body via HEREDOC, metadata via `gh pr create` + `gh api -X PATCH` for milestone 10. Print PR URL.
>
> **PR body template:**
> ```
> Closes #95
>
> Implements spec §4.4 of `docs/superpowers/specs/2026-05-22-v0.6.0-solver-polish-release-design.md`. Bundles the Chunk F `/pr-review` follow-ups that were merged before their threads landed on PR #94.
>
> ## Code/comment fixes
>
> 1. Matplotlib comment in `cmd_solve` rewritten to be honest about what is and isn't deferred.
> 2. `_HUMAN_SHIFT_THRESHOLD_M` replaced with `DiversityConfig().position_threshold_m` reference (one less knob).
> 3. Per-function `§5.x` docstring references dropped (rot surface > value).
>
> ## Test coverage
>
> 4. `exhausted_budget` human-output branch — three new assertions; skip-on-lucky guard.
> 5. Misnamed `test_human_output_exhausted_budget` renamed to reflect actual coverage.
> 6. K>1 happy path with `{i}` substitution exercises the enumerate loop at i=2.
> 7. `--fleet`/`--hangar` override branches now have positive + collision-rejection coverage.
> 8. `OSError`-during-write rc=2 path.
> 9. `maintenance_plane` round-trip via write-then-load.
>
> ## Out of scope
>
> Adjacent observations not in spec §4.4 are filed as fresh issues (none surfaced during implementation) — see issue #115 for the release plan.
> ```

### Task 5 verification (main session, after subagent returns)

Same structure. Applicable `/pr-review` agents: `code-reviewer`, `comment-analyzer` (matplotlib comment + docstring trims), `pr-test-analyzer` (heavy test additions). Likely NOT applicable: `type-design-analyzer`, `geometry-invariant-guard`.

---

## Task 6: Release cut (main session, after Wave 2 lands)

**Files:** managed by the `/release-cut` skill (typically: `pyproject.toml` version bump, release notes, tag).

- [ ] **Step 1: Confirm Wave 2 both merged**

```bash
gh pr view <task4-pr> --json state --jq .state
gh pr view <task5-pr> --json state --jq .state
```

Expected: both `MERGED`.

- [ ] **Step 2: Sync develop**

```bash
git switch develop
git pull --ff-only
git log -5 --oneline
```

Expected: the four merge commits visible at recent history.

- [ ] **Step 3: Reap Wave-2 worktrees**

If still present:

```bash
git worktree remove ../hangarfit-98-fixture-matrix-fail-loud
git worktree remove ../hangarfit-95-chunk-f-cleanup
git branch -d feature/98-fixture-matrix-fail-loud
git branch -d feature/95-chunk-f-cleanup
```

Or use the `commit-commands:clean_gone` skill.

- [ ] **Step 4: Cut the release**

Invoke:

```
/release-cut version=0.6.0
```

The skill creates `release/0.6.0` off `develop`, bumps `pyproject.toml` version, opens a PR into `main` and a back-merge PR into `develop`, drafts release notes (group the four PRs under "Solver polish"), and prepares the `v0.6.0` tag.

If the skill errors (e.g., version-bump confusion because `pyproject.toml` is still at `0.1.0` despite v0.5.0 milestone), do the manual fallback: edit `pyproject.toml` `version = "0.1.0"` → `version = "0.6.0"`, commit on `release/0.6.0`, and proceed.

- [ ] **Step 5: Approve + merge release PRs (user action)**

`/release-cut` hands back two PRs (into `main`, into `develop`). User reviews and merges both.

- [ ] **Step 6: Tag and close milestone (main session, after user merges)**

```bash
git switch main
git pull --ff-only
git tag -a v0.6.0 -m "v0.6.0 — Solver polish"
git push origin v0.6.0
gh api -X PATCH repos/:owner/:repo/milestones/10 -f state=closed
```

Expected: tag `v0.6.0` lands on `main`; milestone closes; gh release notes auto-populate from the merged PR titles + issue links.

- [ ] **Step 7: Update `MEMORY.md` with v0.6.0 progress**

Update `feedback/project_phase2a_progress.md` (or create a new `project_v060_release.md`) with the four PR numbers + merge SHAs + release tag. Pattern follows `project_phase2a_progress.md`'s style.

- [ ] **Step 8: Hand back to user**

> "v0.6.0 — Solver polish shipped. Tag `v0.6.0` on `main`. Milestone closed. Maintenance bay walling (Milestone #9) is the natural next release."

---

## Self-review checklist (post-plan, pre-handoff)

Run through these BEFORE invoking subagents:

- [ ] **Spec coverage:** Every spec section §4.1-§4.4 maps to exactly one Task (1-5). §5 maps to Wave-1/Wave-2 dispatch + Task 3 gate. §6 maps to per-task acceptance gates. §7 maps to Task 6. §8 (out-of-scope) is not implemented; it's the negative-space rule. §9 (risks) is referenced in per-task scope-discipline notes.
- [ ] **Placeholders:** No "TBD", "TODO", "fill in details". Every command is exact. Every `K` value is derived in a documented procedure (Task 4 §4.3 procedure).
- [ ] **Type consistency:** `SolverDiagnostics.diversity_impossible: bool` and `diversity_rejected_count: int` referenced consistently across Tasks 1 and 5. `SearchConfig.max_restarts: int | None` referenced consistently across Tasks 2 and 4. No name drift.
- [ ] **Cross-references:** Issue numbers (#92, #111, #98, #95, #115) and milestone number (10) match GitHub state. PR numbers are placeholders that get filled in at subagent return time.
- [ ] **Workflow conformance:** Per-PR `/pr-review` step in every verification block. Resolve-threads-via-GraphQL step in every verification block. User-only merge in every "Hand back" step. No `gh pr merge` from Claude anywhere.

---

**Next step:** user reviews this plan + the spec together. On approval, commit the plan, open the PR for issue #115 (bundling spec + plan), run `/pr-review` (comment-analyzer-led for a docs-only PR), resolve threads, hand back for merge. After that PR merges, dispatch Wave 1 per Task 0 → Wave 1 setup.
